#!/usr/bin/env bash
set -e
source "$(dirname "$0")/mt-define-mousetrap-home"
AUTHORS_FILE="$MOUSETRAP_HOME/AUTHORS"
echo "# Generated by $0" > "$AUTHORS_FILE"
echo "Commits Author" >> "$AUTHORS_FILE"
git shortlog -s -e -n | sed 's/@/ AT /g' | sed 's/\./ DOT /g' >> "$AUTHORS_FILE"
